<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- floating editText -->
        <item name="textColorError">@color/design_textinput_error_color_light</item>
        <item name="colorControlActivated">@color/colorControlActivated</item>
        <item name="android:textColorPrimary">@color/textColor</item>
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/textColorHint</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorControlNormal">@color/floatHighlight</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--Material button theme-->
    <style name="MyMaterialButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/Theme.MaterialButton</item>
    </style>

    <style name="Theme.MaterialButton" parent="Theme.AppCompat">
        <item name="colorAccent">@color/buttonAccent</item>
        <item name="colorButtonNormal">@color/buttonNormal</item>
    </style>


    <!--Material button theme white-->
    <style name="MyMaterialButtonWhite" parent="Widget.AppCompat.Button.Colored">
        <item name="android:theme">@style/Theme.MaterialButtonWhite</item>
    </style>

    <style name="Theme.MaterialButtonWhite" parent="Theme.AppCompat">
        <item name="colorAccent">@color/buttonAccentWhite</item>
        <item name="colorButtonNormal">@color/buttonNormalWhite</item>
        <item name="android:textColor">@color/buttonNormalGray</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
